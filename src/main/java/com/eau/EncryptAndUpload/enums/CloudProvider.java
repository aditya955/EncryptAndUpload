package com.eau.EncryptAndUpload.enums;

/**
 * Enum representing supported cloud storage providers for the EncryptAndUpload project.
 * <p>
 * Add additional providers to this enum as support for more cloud services is implemented.
 * </p>
 */
public enum CloudProvider {
    /**
     * Google Drive cloud storage provider
     */
    GOOGLE_DRIVE();
}
